'use strict';
app
    .component('finishedTasks',
    {
        templateUrl: 'spa/src/components/finished-tasks/finished-tasks.html',
        controller: function () {
            const $ctrl = this;
        }
    });